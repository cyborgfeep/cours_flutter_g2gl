class Transaction {
  String name;
  String phoneNumber;
  int price;
  DateTime dateTime;
  bool isRetrait;

  Transaction(
      this.name, this.phoneNumber, this.price, this.dateTime, this.isRetrait);
}
